#include "Tools.h"
#include <iostream>
#include <vector>
#include <thread>
#include <conio.h>
#include <Windows.h>

 

int RandomPosition()
{
	//int y = 1 + rand() % 24;
	//int x = 1 + rand() % 24;
	//int XY = 26 * y + x;
	return 1 + rand() % 24;
}